package Game.Entities.Static;

import Game.Entities.EntityBase;
import Main.Handler;

public class LillyPad extends StaticBase {
    public LillyPad(Handler handler) {
        super(handler);
    }
}
